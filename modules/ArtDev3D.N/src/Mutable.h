/// Mutables defines the properties that can be mutated.

#ifndef MUTABLES
#define MUTABLES

namespace Mutable
{
	enum Property
	{
		sequence,
		lifespan,
		promoter,
		thresholds,
		neighbourhood,
		parameters,
		number_of_properties
	};
}

#endif
