#include "Protein_Adjusting.h"

using std::vector;

void Protein_Adjusting::perform_action()
{
	//for (int i = 0; i != this->parameters->size(); i++)
	//	this->host->adjust_hormones((Hormone::Type)i, this->parameters->at(i));
}
