#ifndef PARAMETERS
#define PARAMETERS

namespace Parameters
{
	enum Parameter
	{
		capacity,
		doomsday,
		elitism,
		lifespan,
		mating_rate,
		max_prodigies,
		mutation_rate,
		offspring_rate,
		number_of_parameters
	};
}

#endif
